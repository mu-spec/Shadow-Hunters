# Assets

Place game assets (images, sprites, audio) here for later phases:

- `images/` — sprites, backgrounds
- `audio/` — music, sound effects

The folder is registered in `pubspec.yaml` so the tooling bundles its contents.
