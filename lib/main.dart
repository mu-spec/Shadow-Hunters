// Entry point for the Shadow Hunters app.
//
// Flutter builds expect the top-level entry file at `lib/main.dart`.
// The actual bootstrap logic lives in app/main.dart; this file simply
// forwards to it so the modular `lib/app/` layout is preserved.
export 'app/main.dart';
